<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="WorkforceOS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Standard 4x2 Attendance Widget Receiver -->
        <receiver
            android:name=".AttendanceWidgetProvider"
            android:exported="true"
            android:label="@string/widget_standard_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_IN_WFO" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_IN_WFH" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_OUT" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/attendance_widget_standard_info" />
        </receiver>

        <!-- Compact 2x1 Bar Attendance Widget Receiver -->
        <receiver
            android:name=".AttendanceWidgetCompactProvider"
            android:exported="true"
            android:label="@string/widget_compact_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_IN_WFO" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_IN_WFH" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_OUT" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/attendance_widget_compact_info" />
        </receiver>

        <!-- Full Dashboard 4x3 Attendance Widget Receiver -->
        <receiver
            android:name=".AttendanceWidgetDashboardProvider"
            android:exported="true"
            android:label="@string/widget_dashboard_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_IN_WFO" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_IN_WFH" />
                <action android:name="com.workforceos.mobile.ACTION_CLOCK_OUT" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/attendance_widget_dashboard_info" />
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
